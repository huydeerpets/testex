{{#unless siteSettings.allow_expired_on_all_topics}}
<section class='field'>
  <div class="enable-expired-answer">
    <label class="checkbox-label">
      {{input type="checkbox" checked=category.enable_expired_topics}}
      {{i18n 'expired.allow_expired_topics'}}
    </label>
  </div>
</section>
{{/unless}}
